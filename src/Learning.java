public class Learning {
    public static void main(String[] args) {
        boolean isUnderstood = false;
        while (isHasNextSolution()) {
            if (isUnderstood) {
                // 补习知识点
            } else {
                // 多看几遍，搞懂搞透思想
                // 自己做一遍
                isUnderstood = true;
            }

        }
    }

    private static boolean isHasNextSolution() {
        return true;
    }
}
